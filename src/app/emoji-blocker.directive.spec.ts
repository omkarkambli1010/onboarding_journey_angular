import { EmojiBlockerDirective } from './emoji-blocker.directive';

describe('EmojiBlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new EmojiBlockerDirective();
    expect(directive).toBeTruthy();
  });
});
